# Quick test with dashboard enabled but W&B disabled
training:
  rollout_steps: 512
  total_updates: 10
  checkpoint_interval: 5

model:
  d_model: 256
  n_layers: 2

wandb:
  enabled: false

dashboard:
  enabled: true
  port: 8050